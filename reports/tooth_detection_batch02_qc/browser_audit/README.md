# Temporary Batch 02 visual audit viewer (not production)

From repository root:

```text
streamlit run reports/tooth_detection_batch02_qc/browser_audit/app.py --server.port 8501
```

Then open: http://localhost:8501

Read-only overlay of existing YOLO labels. Does not write annotations.
