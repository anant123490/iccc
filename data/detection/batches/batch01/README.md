# Batch 01 (canonical)

Do not copy or flatten these files here.

| Asset | Path |
|-------|------|
| YOLO dataset | `fdi_detection_dataset/tooth_detector_batch01/` |
| Split | 46 train / 6 val / 8 test images |
| Boxes | 767 verified `tooth` rectangles |
| Weights | `models/detection/tooth_detector_batch01/weights/best.pt` |

Held-out estimates (tiny test set): Precision 0.700, Recall 0.726, F1 0.712, mAP50 0.718, mAP50-95 0.282.

Future batches go in sibling folders (`batch02/`, `batch03/`) with their own `data.yaml` and metadata.
