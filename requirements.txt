# CCC AI Dentist Camera 2.0 — single shared environment
# Python 3.11 recommended (see docker/Dockerfile.backend).
# Install once:  pip install -r requirements.txt
# Do not create extra venvs for Streamlit or YOLO.
#
# Used by: FastAPI backend, Streamlit patient/admin UIs, ml/train.py,
#          Tooth Detector V2 (Ultralytics), Grad-CAM, Groq reports, pytest.
#
# Not included:
#   tensorflowjs / tf2onnx  — optional export only (ml/export.py)
#   crm_backend extras      — that app has its own pyproject.toml
#   CUDA wheels             — pip installs CPU torch unless you choose CUDA

# ---------------------------------------------------------------------------
# API (app/backend)
# ---------------------------------------------------------------------------
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
python-multipart>=0.0.9
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.4.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
httpx>=0.27.0,<1.0.0

# ---------------------------------------------------------------------------
# Database (default: SQLite via DATABASE_URL)
# ---------------------------------------------------------------------------
sqlalchemy>=2.0.0,<3.0.0

# ---------------------------------------------------------------------------
# ICDAS classifier — MobileNetV3 + CBAM (TensorFlow / Keras)
# numpy must stay <2 for this TensorFlow range.
# ---------------------------------------------------------------------------
numpy>=1.24.0,<2.0.0
tensorflow>=2.15.0,<2.17.0
keras>=3.3.0,<3.16.0
scipy>=1.10.0,<1.14.0
scikit-learn>=1.3.0,<1.6.0
pandas>=2.0.0,<3.0.0
albumentations==1.3.1
PyYAML>=6.0.1

# ---------------------------------------------------------------------------
# Images / Grad-CAM / quality checks
# ---------------------------------------------------------------------------
opencv-python-headless>=4.8.0,<4.12.0
Pillow>=10.0.0,<12.0.0
matplotlib>=3.7.0,<4.0.0
seaborn>=0.13.0,<0.14.0

# ---------------------------------------------------------------------------
# Tooth Detector V2 (YOLO11) — inference in FastAPI, training tools unused
# unless you explicitly run tools/train_tooth_detector_*.py
# ---------------------------------------------------------------------------
torch>=2.2.0,<2.6.0
torchvision>=0.17.0,<0.21.0
ultralytics>=8.3.0,<8.4.0

# ---------------------------------------------------------------------------
# Groq dentist-report text (optional at runtime if GROQ_API_KEY is empty)
# ---------------------------------------------------------------------------
groq>=0.9.0

# ---------------------------------------------------------------------------
# Streamlit UIs (patient :8502, admin :8503) — HTTP clients only
# ---------------------------------------------------------------------------
streamlit>=1.32.0,<1.40.0
requests>=2.31.0,<3.0.0

# ---------------------------------------------------------------------------
# CLI helpers
# ---------------------------------------------------------------------------
tqdm>=4.66.0,<5.0.0

# ---------------------------------------------------------------------------
# Tests
# ---------------------------------------------------------------------------
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<0.26.0
