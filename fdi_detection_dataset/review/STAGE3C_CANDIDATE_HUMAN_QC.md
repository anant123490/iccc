# Human QC of candidate tooth boxes (Stage 3C-1)

**Not active yet.** No candidate visualizations or candidate labels were generated because Batch_01 human boxes are not in the repository.

When they exist, review:

`fdi_detection_dataset/review/stage3c_candidate_visualizations/`

Correct missed teeth, extra boxes, overlap, bad boundaries, and false positives. The human is the final authority. Candidates are **not** ground truth.

Do **not** assign FDI or ICDAS during this QC.

Manifest: `reports/stage3c_candidate_review.csv` (`PENDING_HUMAN_QC` when inference has run).
