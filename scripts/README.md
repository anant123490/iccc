# Scripts

Launch helpers (shared `.venv`, ML only on the backend):

- `scripts/start_backend.ps1` / `.bat` — FastAPI :8000
- `scripts/start_patient.ps1` / `.bat` — Streamlit patient :8502
- `scripts/start_admin.ps1` / `.bat` — Streamlit admin :8503

Windows walkthrough: `docs/WINDOWS_SETUP.md`.

Public-dataset ingest lives in `tools/ingest/`.

