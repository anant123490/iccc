# ICDAS production candidates

This directory is empty on purpose.

There is **no** approved 5-class softmax `deploy.keras` yet.

When a model passes evaluation, place it here (for example `deploy.keras`, `best.keras`, or `icdas_mobilenet_cbam_v2/`) **without** overwriting files in `../historical/`.
