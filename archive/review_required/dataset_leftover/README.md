# Empty leftover after ICDAS move

The old `dataset/` directory was emptied when annotations, excluded images, and split folders moved to `data/icdas/`. The 643-row CSV is at `data/icdas/annotations/annotations.csv`.
