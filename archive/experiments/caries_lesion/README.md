# Lesion (d/D) YOLO experiments

Not whole-tooth detection and not ICDAS.

Scripts were moved here from `tools/`. The live FastAPI app still imports `app/backend/app/caries_detector.py` and `caries_pipeline.py` — those files were **left in place** so the API does not break.
