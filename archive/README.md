# Archive

Nothing here is deleted project work. It is isolated so the active tree stays readable.

| Folder | Contents |
|--------|----------|
| `historical/` | Pre-caries-pipeline `.bak` sources; old scripts if moved later |
| `experiments/` | Failed YOLO runs, Ultralytics dumps, lesion `d`/`D` train helpers |
| `obsolete/` | Reserved |
| `out_of_scope/fdi/` | FDI search reports and `fdi_mapping` (FDI is not in the product) |
| `review_required/` | Empty leftover `dataset/` dirs, reorg logs, oddities |

**Do not** move `fdi_detection_dataset/` here. Active detection code still uses that path.
